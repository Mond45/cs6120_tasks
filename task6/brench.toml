extract = 'total_dyn_inst: (\d+)'
benchmarks = '../../bril/benchmarks/**/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}",
]

[runs.ssa]
pipeline = [
    "bril2json",
    "cargo run --bin task6",
    "brili -p {args}",
]
